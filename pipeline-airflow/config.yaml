name: "Pipeline Airflow"
version: "2.13.0"
slug: "pipeline-airflow"
description: "Apache Airflow 3.3 orchestrator for the data-pipeline stack (LocalExecutor)"
url: "https://github.com/hoascv/homeassistant-addons/tree/main/pipeline-airflow"
arch:
  - amd64
startup: application
boot: manual
init: false
map:
  - share:rw
ports:
  8080/tcp: 8085
ports_description:
  8080/tcp: "Airflow web UI"
# Adds an "Open Web UI" button on the add-on page that opens Airflow on its
# mapped host port from within Home Assistant.
webui: "http://[HOST]:[PORT:8080]/"
options:
  admin_user: "admin"
  admin_password: "changeme"
  postgres_host: "172.30.32.1"
  postgres_port: 5432
  airflow_db_password: "changeme"
  spark_master: "172.30.32.1"
  spark_connect_port: 15002
  manage_bundled_dags: true
  minio_endpoint: "http://172.30.32.1:9000"
  minio_access_key: "minioadmin"
  minio_secret_key: "changeme"
  pipeline_pg_user: "pipeline"
  pipeline_pg_password: "changeme"
  pipeline_pg_db: "pipeline"
  gym_tracker_base_url: ""
  gym_tracker_api_token: ""
  coop_tracker_base_url: ""
  coop_tracker_api_token: ""
  detection_hub_base_url: ""
  detection_hub_api_token: ""
schema:
  admin_user: "str"
  admin_password: "password"
  postgres_host: "str"
  postgres_port: "port"
  airflow_db_password: "password"
  spark_master: "str"
  spark_connect_port: "port"
  manage_bundled_dags: "bool?"
  minio_endpoint: "str"
  minio_access_key: "str"
  minio_secret_key: "password"
  pipeline_pg_user: "str"
  pipeline_pg_password: "password"
  pipeline_pg_db: "str"
  gym_tracker_base_url: "str?"
  gym_tracker_api_token: "password?"
  coop_tracker_base_url: "str?"
  coop_tracker_api_token: "password?"
  detection_hub_base_url: "str?"
  detection_hub_api_token: "password?"
